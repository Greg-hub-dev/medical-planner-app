// Aucun accès privilégié n'est exposé au contenu web : l'application se comporte
// comme une application web normale servie en local. Ce fichier est conservé pour
// respecter les bonnes pratiques d'isolation de contexte d'Electron.
window.addEventListener('DOMContentLoaded', () => {
  // Intentionnellement vide.
});
